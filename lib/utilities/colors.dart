import 'package:flutter/material.dart';
import 'package:tinycolor2/tinycolor2.dart';

const baseColor = Color.fromARGB(255, 219, 8, 36);
var primaryColor = TinyColor.fromColor(baseColor).color;
var primaryColorDark = TinyColor.fromColor(baseColor).darken().color;
var darkColor = Color(0xff140F2D);
